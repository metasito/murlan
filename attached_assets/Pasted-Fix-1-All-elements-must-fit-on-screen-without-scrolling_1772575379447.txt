Fix 1: All elements must fit on screen without scrolling in landscape
1.	Open `app/(online)/index.tsx`. The “Con Amici” screen must never scroll in landscape orientation. Change `MenuLayout`’s `scrollable` prop to `false` on this screen specifically. The screen must use `flex: 1` with no `ScrollView` wrapper so the content is forced to fit the available height.
2.	The entire content area must use `useWindowDimensions` to read the current screen height and distribute space proportionally. Do not use fixed `height` or `minHeight` values on any card, row, or button inside this screen.
3.	Reduce the visual scale of elements specifically for this screen using the following rules — do not change the shared `MenuCard`, `MenuButton`, or `MenuLayout` components globally, only apply overrides locally in this screen’s StyleSheet:
•	Reduce vertical `padding` inside the `MenuCard` containers from the default `Spacing.md` (16) to `Spacing.sm` (8)
•	Reduce `marginBottom` between cards from `Spacing.md` (16) to `Spacing.xs` (4)
•	Reduce the chip buttons (Libera/Coppie and 2/3/4 selectors) `paddingVertical` to match the compact size used in the “Gioca VS AI” number chips — they should be square-ish and compact, not tall
•	Reduce the `minHeight` of the `MenuButton` “Crea Stanza” from 52 to 44 on this screen only, by passing a `style` override prop with `minHeight: 44` directly on that button instance
•	Reduce the `gap` between chip rows from `Spacing.sm` (8) to `Spacing.xs` (4)
4.	The “Connesso come [username]” status row at the top must have reduced `marginBottom` — use `Spacing.xs` (4) instead of any larger value.
5.	The two-column layout (CREA STANZA left, ENTRA IN UNA STANZA right) must use `alignItems: 'stretch'` on the row container so both cards stretch to the same height, and both columns use `flex: 1` with no fixed height set anywhere.
6.	After these changes, verify in landscape orientation that no element is cut off and no scrolling is triggered. The “Crea Stanza” button must be fully visible without scrolling.