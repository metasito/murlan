# Lessons

Rules that change future behaviour, learned by running the loop. One line each:
`- [#<issue>] <imperative rule>`. A rule that only narrates, or that the code
already tells you, does not belong here.

Cap: 40 lines. On overflow, merge duplicates and drop what is no longer
load-bearing. A bloated lessons file is the same failure as a bloated context.
