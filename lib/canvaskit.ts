/** The `canvaskit-wasm` @shopify/react-native-skia pins (tests/ui-rules/feltWeave.test.ts). */
export const CANVASKIT_VERSION = "0.41.0";

/** Where the web felt fetches CanvasKit's wasm; the server's CSP allows exactly this path. */
export const CANVASKIT_URL = `https://cdn.jsdelivr.net/npm/canvaskit-wasm@${CANVASKIT_VERSION}/bin/full/`;
